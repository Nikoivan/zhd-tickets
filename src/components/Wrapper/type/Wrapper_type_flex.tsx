import { FC, PropsWithChildren } from 'react';

import './Wrapper_type_flex.scss';

const WrapperTypeFlex: FC<PropsWithChildren> = ({ children }) => <div className='Wrapper_type_flex'>{children}</div>;

export default WrapperTypeFlex;
